<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <!-- Displays the header component in the includes folder-->
    <%- include('includes/header') %> <%- include('includes/burger-menu') %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if (numberOfRestaurants > 0) { %> <% if (numberOfRestaurants === 1) {
      %>
      <p>We found <%= numberOfRestaurants %> restaurant</p>
      <% } else { %>
      <p>We found <%= numberOfRestaurants %> restaurants</p>
      <% } %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <ul id="restaurants-list">
        <!-- The first restaurants is the key in the rest.-item file,
        the second is the const variable below -->
        <% for (const restaurant of restaurants) { %> <%-
        include('includes/restaurants/restaurant-item',
        {restaurants:restaurant}) %> <% } %> <% } else { %>
        <p>Please enter some restaurants</p>
        <% } %>
      </ul>
    </main>
  </body>
</html>
